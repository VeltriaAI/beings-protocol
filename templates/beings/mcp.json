{
  "servers": {
    "axon": {
      "command": "axon",
      "args": ["serve", "--watch"],
      "description": "Code intelligence — structural search, impact analysis, dead code detection"
    },
    "basic-memory": {
      "command": "basic-memory",
      "args": ["mcp", "--project", "default", "--transport", "stdio"],
      "description": "Markdown-native persistent memory — knowledge graph, semantic search, git-syncable"
    }
  }
}
